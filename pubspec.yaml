name: cli_task_manager
description: A command-line task manager written in pure Dart with local JSON persistence.
version: 1.0.0

environment:
  sdk: ^3.12.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
